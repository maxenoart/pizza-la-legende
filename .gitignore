# env.js wird bewusst MITVERÖFFENTLICHT: es enthält nur die öffentliche
# Supabase-URL + den publishable Key (durch RLS geschützt) und muss von
# GitHub Pages ausgeliefert werden, damit die Live-Seite Supabase erreicht.
# NIEMALS den secret / service_role Key in dieses File schreiben.

# Secrets serveur — ne jamais committer
.env
.env.*
supabase/.env

# Système
.DS_Store
Thumbs.db

# Dépendances / build éventuels
node_modules/
